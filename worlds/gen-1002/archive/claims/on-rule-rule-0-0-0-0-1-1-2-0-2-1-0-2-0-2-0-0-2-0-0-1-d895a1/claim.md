On rule RULE=[0,0,0,0,1,1,2,0,2,1,0,2,0,2,0,0,2,0,0,1,2,0,2,1,0,0,1] (radix-3 idx=9*left+3*center+right) over cyclic tapes of width p≥3 with an all-0 background: a single '2' defect (tape all 0 except one cell =2) is an exact fixed point — step(tape)==tape, mass stays exactly one nonzero cell forever. A single '1' defect (tape all 0 except one cell =1) is not fixed but translates rigidly: step(tape) equals tape cyclically shifted right by exactly one cell (new[i]=old[(i-1) mod p]), so it produces an exact period-p limit cycle (returns to original after exactly p steps), again with mass conserved at exactly one nonzero cell every step. Both behaviors hold for every p from 3 to 30 and for every starting position (by translation symmetry of the cyclic tape); p=1,2 are degenerate (neighbor wraparound makes left==right==center's neighbor coincide with the defect itself) and excluded.
