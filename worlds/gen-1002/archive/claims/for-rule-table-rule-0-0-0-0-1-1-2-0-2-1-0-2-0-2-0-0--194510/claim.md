For rule table RULE=[0,0,0,0,1,1,2,0,2,1,0,2,0,2,0,0,2,0,0,1,2,0,2,1,0,0,1] (radix-3 index = 9*left+3*center+right) on cyclic tapes of any width n≥1: the all-0 tape is a strict fixed point (step(0^n)=0^n, since RULE[0]=0); the all-1 and all-2 tapes form a strict period-2 cycle under step (step(1^n)=2^n and step(2^n)=1^n, since RULE[13]=2 and RULE[26]=1), with minimal period exactly 2 (never collapsing to period 1, since 1≠2). This holds for every width n=1..60 (verified exhaustively), and follows directly from homogeneous-neighborhood lookups (idx 0, 13, 26) being width-independent, so it holds for all n≥1 by construction.
