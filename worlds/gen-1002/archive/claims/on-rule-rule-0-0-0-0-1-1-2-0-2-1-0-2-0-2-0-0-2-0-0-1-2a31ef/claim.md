On rule RULE=[0,0,0,0,1,1,2,0,2,1,0,2,0,2,0,0,2,0,0,1,2,0,2,1,0,0,1] (radix-3, idx=9*left+3*center+right) over an all-0 background on a cyclic tape: a single isolated defect (one cell set to `1`, all else `0`) is NOT a fixed point — it moves ballistically rightward at constant velocity +1 cell/step indefinitely (position at time t = position at t=0 + t, always shape `(1,)`, never spawning/decaying). Consequently two such lone-`1` defects placed with initial gap g (cells between them) on the same background move at identical velocity and never collide/interact if g≥3 (relative spacing stays constant forever, both survive as separate `(1,)` clusters); but if g≤2 the neighborhoods overlap at step 0 (radius-1 rule) and they immediately merge/annihilate down to a single `(1,)` cluster within the first few steps. This gives an exact mechanical explanation (ballistic transport + radius-1 interaction range) for the earlier gap-dependent merge/no-merge observations.
