For rule table RULE = [0,0,0,0,1,1,2,0,2,1,0,2,0,2,0,0,2,0,0,1,2,0,2,1,0,0,1] (radix-3, index = 9*left+3*center+right): (1) a tape of all-0 is an exact fixed point; (2) a single-1 defect on an all-0 background (all other cells 0) moves right by exactly one cell per step at constant speed 1, preserving its single-cell shape indefinitely (until wraparound); (3) a single-2 defect on an all-0 background is an exact static fixed point (stays in place forever); (4) when a right-moving single-1 glider (starting left of a static single-2 obstacle, same 0-background, gap ≥2) reaches the obstacle, it is annihilated on contact and the system settles permanently into a single static 2 at position (obstacle_start_pos − 1), with no other nonzero cells, for all further steps — this holds regardless of the initial glider-obstacle gap (tested gaps 3–15) and tape width (tested N=41,61,81).
