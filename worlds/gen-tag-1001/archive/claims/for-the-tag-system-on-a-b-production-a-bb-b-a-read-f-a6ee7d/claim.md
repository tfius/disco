For the tag system on {a,b} (production a→bb, b→a; read first symbol, delete first two, append production), every word tested halts — no cycling or budget-exceeding trajectory has been found. Exhaustive check over all 2^L words for L=1..14, and random sampling (seed 12345, 2000 words per length) for L∈{15,20,30,50,80,120,200} with step budget 3000, gives 100% halting, 0 cycles, 0 unknowns. Mechanism: length changes by 0 when first symbol is 'a' (delete 2, append 2) and by -1 when first symbol is 'b' (delete 2, append 1), so length is non-increasing; empirically the "a-runs" (constant-length stretches) never loop back to a repeated word, so every trajectory eventually shrinks to length <2 and halts, within the tested regime.
