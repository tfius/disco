For the tag system on {a,b} with production a->bb, b->a (read first symbol, delete first two symbols, append production, halt when word length <2): every word w of length L, for all 1<=L<=20, halts within 2L-2 steps, and this bound is tight — the maximum halting step count over all 2^L words of length L is exactly 2L-2 (0 when L=1). No word of length <=20 cycles or exceeds a 5000-step budget.
