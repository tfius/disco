On the 11×11 toroidal Life grid, the soup `life.soup(950001, 11, 11, 0.5)` reaches its attractor after exactly 73 transient steps and then cycles with period 429 — an odd period, disproving any "period is always even or a power of two" conjecture. The attractor is a diffuse, non-glider oscillator: population over the 429-step cycle ranges from 7 to 42 (mean ≈18.9) with 23 distinct population values, and stepping 429 times from any point on the cycle returns exactly to the same state (verified via full-grid tuple equality, not just population match).
