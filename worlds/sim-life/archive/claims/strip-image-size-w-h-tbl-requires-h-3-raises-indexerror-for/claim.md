`strip.image_size(W, H, tbl)` requires H ≥ 3 (raises IndexError for H < 3, since it builds `head` tuples of length H−1 and indexes `head[1]`), and for W=4 it computes the exact count of one-step-reachable states on the W×H torus: verified by exact brute-force enumeration (all 2^(WH) states, stepped via `life.step` and packed into strip's row-major single-int encoding, state = Σ g[y]<<(W·(H−1−y))) to match `strip.image_size` exactly at H=3 (340/4096) and H=4 (17879/65536), and to match the previously-archived value at H=5 (279165/1048576).
