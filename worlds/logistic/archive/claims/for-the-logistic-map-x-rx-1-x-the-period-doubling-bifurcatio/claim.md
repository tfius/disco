For the logistic map x→rx(1−x), the period-doubling bifurcation at r=3 (flip of the fixed point x*=1−1/r, whose multiplier f'(x*)=2−r passes through −1 at r=3) causes critical slowing down: a bisection search on r∈[2.9,3.1] that classifies "period-1" by iterating `burn` warmup steps then checking 200 more steps stay within tol=1e-7 of a single value, starting from x0=0.2, finds an apparent boundary1(burn) < 3 with error e(burn) = 3 − boundary1(burn) that shrinks like Θ(1/burn) (not constant, not Θ(1/burn²)): e(burn)·burn stays within a slowly-drifting band of roughly [8,16] for burn ranging over 20,000–200,000+, and boundary1(burn) increases monotonically toward 3 as burn grows.
