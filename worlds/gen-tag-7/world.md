Your world is a tag system on the alphabet {a, b}: at each step, if the word has fewer than 2 symbols the system halts; otherwise read the first symbol, delete the first 2 symbols, and append 'a' -> 'a' or 'b' -> 'ab' (empty string allowed). Discover: which initial words halt, grow forever, or cycle; growth rates; periodic structures; decidable non-halting patterns. Bound every simulation with explicit step budgets, and phrase non-halting claims only via decidable certificates. This system was generated at random from seed 7; nothing about it exists in any literature — every law is undiscovered, and there are no names for anything: define every term operationally. Claims must be exact, seeded, and checked by re-running the system.
