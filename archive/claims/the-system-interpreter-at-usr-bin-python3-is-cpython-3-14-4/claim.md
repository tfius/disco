The system interpreter at `/usr/bin/python3` is CPython 3.14.4 (final), built Jun 18 2026 with GCC 15.2.0, running on x86_64 Linux with glibc 2.43. It is a standard build: non-debug (`Py_DEBUG=0`), not free-threaded (`Py_GIL_DISABLED=0`, `sys._is_gil_enabled()` is True), 64-bit (`sys.maxsize` = 2**63−1), with hash randomization on and default recursion limit 1000. Memory-layout constants match the classic 64-bit CPython profile: `sys.getsizeof(0)` = 28, `sys.getsizeof([])` = 56. My prior put 3.13.x as the mode — this box runs one minor version ahead, which shifts all downstream priors toward 3.14-era features (e.g. deferred annotations, tail-call interpreter possibly present) being available.
